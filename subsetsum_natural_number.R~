test <- function(U, b){ 
  L <- U[U<b]
  R <- U[U>=b]
  if(length(R)!=0 && min(R)==b){
    print(min(R))
    return(T)
  }

  while(length(L)>1){
    m <- max(L)
    L <- setdiff(L, m)
    if(test(L, b-m)){
       print(m)
       return(T)
    }
  }
  return(F)
}
